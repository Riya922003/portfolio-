"use client";

// This file was a legacy JS implementation of the Particles component.
// The canonical, typed implementation lives in `Particles.tsx`.
// Re-export the TSX component so existing imports continue to work.

export { default } from './Particles';
