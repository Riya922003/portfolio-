"use client"

// Re-export canonical NewProjectCarousel from the top-level components folder
"use client"

// Re-export canonical NewProjectCarousel from the top-level components folder
export { default } from '../../components/NewProjectCarousel'
